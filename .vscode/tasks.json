{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "cppbuild",
            "label": "Build Tics to Bin (m32)",
            "command": "/usr/bin/g++",
            "args": [
                "-g",
                "-m32",
                "${file}",
                "${workspaceFolder}/Tics.cpp",
                "-I${workspaceFolder}",
                "-o",
                "${workspaceFolder}/bin/${fileBasenameNoExtension}"
            ],
            "options": {"cwd": "${workspaceFolder}"},
            "problemMatcher": ["$gcc"],
            "group": {"kind": "build", "isDefault": true}
        }
    ]
}